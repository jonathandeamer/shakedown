#!/usr/bin/env bash
# Fast full-parity harness entry: IR interpreter + Slice-3 link rewrite.
# Used by automated tests and strict/smoke scripts. Not the public art path —
# for the committed play, use ./shakedown or: uv run shakespeare run shakedown.spl
set -euo pipefail

DIR="$(cd "$(dirname "$0")" && pwd)"
exec uv run --directory "$DIR" python -m scripts.release_runtime

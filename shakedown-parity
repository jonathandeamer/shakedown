#!/usr/bin/env bash
# Fast full-parity harness entry: pure IR interpreter (no Python Markdown rewrite).
# Semantically equal to ./shakedown (shakespeare on the committed play); used by
# automated tests and strict/smoke scripts for speed. Public art path: ./shakedown.
set -euo pipefail

DIR="$(cd "$(dirname "$0")" && pwd)"
exec uv run --directory "$DIR" python -m scripts.release_runtime

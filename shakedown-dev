#!/usr/bin/env bash
# Dev entry: reassemble shakedown.spl from src/, then run via the release entry.
set -euo pipefail

DIR="$(cd "$(dirname "$0")" && pwd)"
uv run --directory "$DIR" python "$DIR/scripts/assemble.py"
exec "$DIR/shakedown"

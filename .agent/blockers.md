# Active blockers

None.

Next unchecked step: **Task 2L Step 2d** (A4.6 ladder) — candidate capture +
replay (`BRACKET`/`LABEL`/`COLON`/`URL_WS`/`URL_GUARD`/`ANGLE`/`URL`/`TITLE`/
`TITLE_NL`/`REPLAY` per A4.3). Success: well-formed definition lines vanish from
the body; Rosalind table may still be empty (case-fold / angle / next-line title
stay red until 2e).

Task 2L Step 2c (line machine) completed 2026-07-20: NEXT/LEAD/FOUR_SPACE/NL/KEEP
keep every line as body; LEAD counts 0–3 spaces and rejects on the fourth.
`USE_ACT1_REF_INTRINSIC` remains False.

# Active blockers

- BLOCK[plan]: Task 2L Step 2 pure-op Act I strip — re-verified 2026-07-20 (claude-implement): confirmed via `scripts/splc/ir.py` `scene()` that each Act I scene carries exactly one `companion` (two value cells: current + companion), while the oracle `apply_act1_reference_strip` (`scripts/splc/act1_ref_strip.py`) needs simultaneously-live state the two-cell budget cannot hold — a `mode` selector across 15 modes, a `remaining` counter, a growing `horatio` capture buffer, `ov`/`pv` counters, and (for `title_nl`) a full save/restore of source position plus string ops (`rfind`, case-fold, entity-escape) with backtrack-on-reject. This is not a mechanical porting gap; it requires inventing a new register/label transfer-bridge choreography (which scenes own which state, how backtrack restores source position, how rfind/fold decompose into character ops) that does not exist in Amendment A1.3 today. Per CLAUDE.md's SPL literary protocol, implementation agents must not invent SPL architecture/literary surfaces mid-task — this choreography must be designed and the A1.3 label table amended at planning time before an implementation session can execute it mechanically. Oracle remains scripts/splc/act1_ref_strip.py. USE_ACT1_REF_INTRINSIC stays False (Step 1 red: 6 failed, 2 passed). Do not re-enable OPEN intrinsic as a Step 2 substitute. Next: `superpowers:writing-plans` amendment to Amendment A1.3 defining the transfer-bridge register/label choreography, then resume Task 2L Step 2 as mechanical implementation against the amended design.

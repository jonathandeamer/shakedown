# MCO Agent Blockers Log
- BLOCK[plan]: Task 4 Step 2 is still unimplemented at committed HEAD (`9be34ef`, which lands only the A9 `LYRIC_TEXT_END_DISPATCH` scaffold — `uv run pytest tests/test_act3_contracts.py -q` is `8 failed, 26 passed`, all eight failures being the still-unimplemented Task 4 render/shape contracts: `test_act3_ir_requeue_and_field_floors_follow_a4_shape` plus the four `does_not_yet_render_expected_span_html` cases plus the three `renders_*` contracts). A fresh design review while attempting reconstruction (no production IR was edited this session — this is a research-only handoff) found one gap in Amendments A2–A9 that blocks a correct A2 "shared field pipeline" implementation for images specifically: the accepted design's `LYRIC_DEST_OPEN`/`LYRIC_TITLE_OPEN` row says destination and title are captured and *emitted* to Juliet immediately, in source order, before the label/alt requeue. That is correct for links (oracle order is `href`, `title`, then requeued label, matching source order exactly: `<a href="D" title="T">label</a>`). It is provably wrong for images: the oracle fixture `links_images_protected.expected` requires `<img src="img.png" alt="c <em>d</em>" title="i" />` — i.e. attribute order `src`, `alt` (the *requeued*, source-earliest bracket content), `title` (captured *after* the destination, source-later than alt) — so title cannot be emitted at capture time for images; it must be held and emitted *after* the alt requeue resumes, while alt (captured earlier in source than title) must still be requeued in between. Proposed resolution for the next planning amendment: for `FIELD_IMAGE_TITLE` only, capture raw title bytes onto Romeo above its own private `STREAM_END` floor without draining/emitting them (skip the normal `LYRIC_FIELD_RETRY` reverse-and-emit call), leave that floor intact under the subsequent alt-requeue's own (independently floor-scoped) Romeo usage, and add one new close-only scene reached from the image branch of `LYRIC_REGION_RESUME` that reverses/entity-encodes/drains exactly that held title floor to Juliet before emitting `" />`. This needs 1 additional working scene name beyond the Amendment A2/A8 pools (all ten A2 spares are otherwise already committed to A7's adapters) plus explicit confirmation that nesting a second private Romeo floor under an in-flight alt/label requeue's own transient Romeo use is legal (each user drains back to its own floor and never reads below it, so it should compose, but this has not been proven by a focused interpreter test). Separately, but not blocking: every one of the three still-red protected-render fixtures embeds emphasis inside the target feature (`<span>*raw*</span>`, `[a *b*]`, `![c *d*]`, `**outer *inner* outer**`), so `test_act3_renders_inline_html_and_autolink`/`_links_images_protected`/`_overlapping_emphasis` cannot pass without the emphasis machine landing too — but `escapes_and_overlap` (`***both***`) and `overlapping_emphasis` (`***both*** and **outer *inner* outer**`) need *only* emphasis, no HTML/link/image, so implementing Amendment A2's emphasis family first (register plan: PROSPERO holds the opening delimiter code 42/95 for the duration of one match, HECATE the opener run length, MACBETH the candidate/matched run length — mirroring Amendment A1's proven code-span register choreography exactly — HORATIO holds unprocessed body glyphs including folded-in non-matching candidate delimiter runs, requeued through the shared A9 `LYRIC_REQUEUE_OPEN`/`LYRIC_REQUEUE_DRAIN` family) is the highest-leverage next sub-step: it is self-contained (no legacy `[` Slice-1 anchor-scene interaction, no title-ordering problem) and flips two of the five span fixtures to fully green on its own. Flag for the next implementation attempt: the reserved Amendment A2 emphasis pool (`LYRIC_EMPHASIS_OPEN`, `_COUNT_MORE`, `_SEEK`, `_CAND_COUNT`, `_CAND_MORE`, `_COMPARE`, `_MATCH`, `_RESUME`, `_FALLBACK`, `_REPLAY`) has no separate "seek-init" or "count-loop" label the way Amendment A1's code-span pool did (`LYRIC_CODE_SEEK_OPEN`, `LYRIC_CODE_COUNT` were both distinct from `LYRIC_CODE_RUN`); an implementer must fold that setup into `LYRIC_EMPHASIS_OPEN`/`_COUNT_MORE` via self-looping rather than inventing new labels, or request one more spare in a planning amendment before writing IR.

# MCO Agent Blockers Log

- BLOCK[plan]: 4S Task 4 Step 2 (protected HTML/link/image regions + strong-then-emphasis) cannot fit the reserved literary scene-title budget. The plan's Task 4 pool reserves 23 working + 6 spare scene titles (29 total) for HTML tag/autolink, link/image label/destination/title, and strong/emphasis. A prior uncommitted attempt (found at session start, `src_ir/act3.py` +678 lines) implemented HTML tag scanning, autolink with full amp/lt/gt-encoded href+text passes, a link/image label/destination/title scanner, and a strong-then-emphasis matcher reusing the Amendment A1 two-character register technique — but needed 91 distinct new `LYRIC_*` scene labels (verified by `git diff | grep -oP '"LYRIC_\w+"' | sort -u | wc -l` => 91), over 3x the reserved budget. Root cause: the WIP duplicates the amp/lt/gt entity-encode-and-resume idiom once per protected-region exit point (separate `_AMP`/`_LT`/`_GT` scenes for HTML tag emit, autolink href, autolink text, link-region href, link-region text, link-region title, emphasis body — 6+ near-identical encode triples) instead of sharing one encode set, and gives link/image label/destination/title each their own open/scan/keep/close/fallback quartet. The WIP also had a real correctness bug (now understood, not yet fixed upstream): `LYRIC_EMPHASIS_EMIT_OPEN`/`_CLOSE` branched on `val(HECATE)` for the delimiter run length, but `LYRIC_EMPHASIS_EMIT_BODY` reuses `pop(HECATE, ...)` as the content-glyph register in between, clobbering the run length before close-tag emission (`LYRIC_EMPHASIS_EMIT_CLOSE: fell off scene without a jump` when body draining left `val(HECATE) == STREAM_END`); the fix is to branch on `val(MACBETH)` instead, which holds the same matched run length and is untouched by the body-draining loop — this fix is safe to reapply once the design fits budget. Also found and fixed in this iteration (not part of the over-budget count): a dangling `else_="LYRIC_TEST_AMPERSAND"` reference in `LYRIC_POP_GLYPH` where the WIP had deleted the `LYRIC_TEST_AMPERSAND` scene body while repurposing its position for `LYRIC_HTML_TAG`; not committed since it's moot without the rest of the WIP. Per the plan's explicit "if either spare pool is exhausted, stop and request a planning amendment instead of inventing prose" rule and this project's established Step-5 precedent (20 prior BLOCK[plan] halts for the same class of issue), the WIP was stashed rather than pushed (`git stash list` entry "Task 4 Step 2 WIP: HTML/autolink/link/image/emphasis scanner, 91 scenes vs 29 reserved budget") and the working tree left at the last known-good commit 713b2f2 (re-verified: `uv run pytest tests/test_act3_contracts.py -q` => `11 failed, 15 passed`, matching the recorded Task 4 Step 1 red-gate baseline exactly). Resolving this requires an interactive planning session to write an Amendment A2 that either (a) redesigns the protected-region scanner to share one amp/lt/gt-encode-and-dispatch idiom and one label/scan/keep/close idiom across HTML/autolink/link/image/title (likely fits close to the existing 29-title budget), or (b) grows the Task 4 scene-title budget to match a duplicated design. Not another autonomous fix/implement attempt — apply the stashed WIP's structure and the MACBETH fix above once the amendment lands.
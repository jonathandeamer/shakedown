# Active blockers

None.

Next unchecked step: **Task 2L Step 2b** (A4.6 ladder) — paste Amendment A4.4
Rosalind recall prose into `src/literary.toml` under
`[characters.rosalind.recall]`. Gate: literary/validate/generated green with no
IR behaviour change (`tests/test_act1_references.py` stays **2 passed, 6 failed**).

Task 2L Step 2a (orientation) completed 2026-07-20: body text is no longer
reversed; keep-all + trailing-NL policy run on pure ops with
`USE_ACT1_REF_INTRINSIC = False`.

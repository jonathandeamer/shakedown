# MCO Agent Blockers Log
- BLOCK[plan]: Task 2 Step 2's Act II HR/code-block gate needs a one-target-per-scene rewrite that exceeds the reserved Act II literary pool. `uv run python -m scripts.splc` fails in `scripts/splc/validate.py::participants` because five scenes each perform Let/Push/Pop assignments against two or three distinct off-anchor characters in one scene body (SPL requires the assignment target on stage, one companion at a time; only branch-tested/RHS-read characters may stay off-stage by name per `docs/spl/reference.md`): `PASS_HR_GATE` (Horatio then Puck targets, needs +1 scene), `PASS_CODE_GATE` (Horatio then Hecate targets, +1), `PASS_HR_SCAN` (Puck, Macbeth, then Hecate targets, +2), `PASS_HR_CONFIRM` (Macbeth then Hecate targets, +1), `PASS_CODE_OPEN` (Horatio then Hecate targets, +1) — 6 new scene labels required. The Act II working+spare pool reserved in `docs/superpowers/plans/2026-07-14-slice-2-low-risk-fixtures.md` has only one unused spare left (`PASS_CODE_BLANK`); the other three spares (`PASS_HR_FALLBACK`, `PASS_CODE_REPLAY`, `PASS_BLOCK_FINISH`) are already consumed as working scenes, leaving a shortfall of 5 titles. Five other violating scenes (`PASS_HR_SPACE`, `PASS_HR_REPLAY`, `PASS_BLOCK_RETURN`, `PASS_CODE_CLOSE`, `PASS_BLOCK_FINISH`) need only a `companion=` correction to an already-declared character (no new prose) and can be folded into the same amendment. Needed: a planning amendment to the Slice 2 plan (or its design) reserving 5+ additional Act II scene titles (plus any Recall labels the split needs) using the shared-idiom/state-family method from Amendment A2/A17-style pools, then re-dispatch Task 2 Step 2 to an implementation session.

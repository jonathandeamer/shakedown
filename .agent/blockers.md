Resolved 2026-07-18 by Slice-4 design Amendment A10 and the matching Task-4-Step-2 amendment: A9's one-level Hecate adapter now selects a private `_LOOSE_COMMIT_SIB_HECATE` mode.  The single newly authorized `PASS_CONTAINERS_DEPTH_SKIP_TAIL` scene copies exactly the first (nested-tail) `ITEM_START` through the existing container transaction, switches to the existing sibling selector, and lets the unchanged scan rewrite only the exposed outer item.  No token, grammar, participant, Act-III/IV surface, compiler/validator behavior, or spare-pool entry changes; the derived ledger is 24 working labels with A5's five unused spares.  A second skip, another scene, a spare draw, or any broader surface remains `BLOCK[plan]`.
Resolved 2026-07-18 by Slice-4 design Amendment A11 and the matching Task-4-Step-2 amendment: retain A10's first-sentinel entry, then preserve the complete immediate nested-list segment through its `LIST_OPEN` boundary under private `_LOOSE_COMMIT_SIB_HECATE_TAIL` before restoring existing `_LOOSE_COMMIT_SIB` for the outer rewrite. The bounded repair reserves two working scenes and a sixth unused Act-II spare (26 working / 6 spare); it changes no token, grammar, participant, Act-III/IV surface, compiler, or validator. A recursive boundary counter, another working scene, spare use, or broader surface remains `BLOCK[plan]`.
Resolved 2026-07-18 by Slice-4 design Amendment A12 and the matching Task-4-Step-2 amendment: preserve A11's direct nested segment through the depth scan, carry a private close-tail selector to the existing close transaction, and replay that segment until its immediate `LIST_OPEN` before restoring the existing outer-item stop behavior. The bounded repair adds exactly two Act-II close-side scenes and no spare draw (28 working / 6 unused spares); it changes no token, grammar, participant, Act-III/Act-IV surface, compiler, or validator. A recursive boundary counter, a second nested `LIST_OPEN`, another working scene, a seventh spare, or broader surface remains `BLOCK[plan]`.
- BLOCK[plan]: Task 4 Step 3 ("Enable and checkpoint full lists") cannot pass its evidence gate as written. Adding "Ordered and unordered lists" to `_IMPLEMENTED_FIXTURES` and running `uv run pytest tests/test_architecture_spikes.py tests/test_mdtest.py -q` fails `test_mdtest[Ordered and unordered lists]`: the checked-in mdtest corpus (`~/mdtest/Markdown.mdtest/Ordered and unordered lists.xhtml`) renders tight nested `<ul>`/`<li>` on separate lines (e.g. `<ul>\n<li>Tab`), while the installed strict oracle (`~/markdown/Markdown.pl` 1.0.2b8) and Shakedown's implementation both emit them on one line (e.g. `<ul><li>Tab`) — confirmed via `uv run python scripts/strict_parity_harness.py 'Ordered and unordered lists'` reporting `1/1 byte-identical` against the live oracle, so the implementation is correct against the strict-byte authority but diverges from the static checked-in fixture. This is the same category of corpus-vs-installed-oracle mismatch that Slice-4 design Amendment A1 resolved for `Nested blockquotes` by declaring the installed oracle authoritative over the checked-in corpus for that one fixture; no equivalent amendment exists for `Ordered and unordered lists`, and the plan's global constraint forbids changing expected fixture files or `test_mdtest.py` normalization without plan authorization. Needs an interactive planning amendment (analogous to A1, or a documented `docs/markdown/divergences.md` entry) naming which authority governs this fixture's nested tight-list serialization and, if the installed oracle is chosen, authorizing the corresponding `test_mdtest.py` normalization. Working tree left clean (`tests/test_mdtest.py` edit reverted); no production files changed this iteration.
